# 版本 20260112.v2 更新说明 (Release Notes)

## 核心变更：稳定化与标准化 (Stability & Standardization)

本次更新主要集中在系统稳定性修复、代码库清理以及建立标准化的发布/部署流程。重点解决了客户数据加载错误和前端 UI 刷新不及时的问题。

### 1. 业务功能增强与修复 (Features & Fixes)
- **[Fix] 客户列表修复**: 修复了 `GET /api/clients` 接口因引用已废弃 Schema 字段导致的 500 错误，现在客户列表及 AUM 计算功能恢复正常。
- **[Fix] 资产添加体验**: 修复了添加外部资产时，列表暂时显示 "Unknown Fund" 的问题（通过完善 `AddAssetModal` 的乐观更新逻辑）。
- **[Fix] 流动性视图刷新**: 解决了在修改持仓流动性规则后，右侧详情面板（显示 T+N、到期日等）未能立即响应最新规则的问题。

### 2. 后端架构与基建 (Backend & Infra)
- **[Cleanup] 深度清理**: 删除了 `inspect_db.py`, `debug_data.py` 和不再兼容的 `seed_portfolio.py`，降低了维护干扰。
- **[Script] 种子工具升级**: 升级了 `backend/generate_seed.py`，现在支持导出 `external_products`, `cash_flows`, `recurring_rules` 等全量表数据，确保测试环境数据的一致性。
- **[Migration] 迁移对齐**: 更新了 `backend/apply_migration.sql`，补全了 `funds` (manager, risk_level) 和 `accounts` (description) 等表的新增字段定义。
- **[Schema] 严格校验**: 经核查，数据库 `schema.sql` 移除了不支持的 `cash_flows.client_id` 字段，确保了设计与实现的一致。

### 3. 文档与流程 (Documentation & Process)
- **[Standard] 发布标准化**: 创建了 [RELEASE_CHECKLIST.md](./RELEASE_CHECKLIST.md)，定义了包含数据库、API、部署检查在内的发版 SOP。
- **[Deploy] 部署指南更新**: 重写了 [QUICK_DEPLOY.md](./QUICK_DEPLOY.md) 的“数据库初始化”章节，清晰区分了“首次部署/全量重置”与“版本更新”的操作命令。
- **[Doc] API 文档同步**: 更新了 [API_DOCS.md](./backend/API_DOCS.md)，补充了账户管理 (Account) 接口，并未为了准确性移除了尚未实现的参数描述。

---
> **发布操作提示**:
> 建议在部署前执行 `RELEASE_CHECKLIST.md` 中的检查项，并参考 `QUICK_DEPLOY.md` 进行数据库初始化（如需重置）。
