# 版本 20260111.v1 更新说明

## 核心变更：周期性流动性统计 (Period Statistics & UX)

本次更新重点在于数据洞察的深度挖掘与交互体验的优化。我们在详情面板增加了全新的“周期性报表”板块，帮助用户从周、月、年等更宏观的维度掌控资金流向，同时对详情面板的滚动交互进行了全面优化。

### 1. 新增功能：周期性流动性报表 (Period Statistics Panel)
- **多维度统计切换**：在超长时间流动性预测页面右侧，新增了统计板块。用户可以一键切换 **周度 (Weekly)**、**月度 (Monthly)**、**年度 (Yearly)** 视图。
- **核心风险指标**：
    - **最低水位 (Min Liquidity)**：自动计算该周期内的最低可用资金额。一旦跌破 0 元，系统将以红色高亮示警，帮助用户快速识别未来的资金缺口风险点。
    - **净收支 (Net Flow)**：聚合展示该周期内的资金总流入与总支出之差，直观反映该时间段的资金蓄水能力。

### 2. 交互优化 (UX Improvements)
- **详情面板滚动重构**：
    - **统一滚动视口**：针对右侧信息量逐渐丰富的情况，我们将整个详情面板（含资金快照、周期报表、资产明细）重构为统一的滚动容器。
    - **溢出体验优化**：移除了内部资产列表（可用流动性、锁定资产）的独立滚动条，消除了“双重滚动条”带来的割裂感，浏览体验更加流畅自然。
    - **自适应高度**：面板高度现在会自动适应屏幕，确保底部内容（如预计支出）始终可以通过简单的整体滚动查看，不再被截断。

### 3. 系统优化 (System Improvements)
- **数据结构升级**：在底层的预测数据模型中补充了流入 (`inflow`) 字段，为后续更复杂的收支分析奠定了数据基础。
- **代码结构优化**：重构了聚合逻辑 (`useMemo`) 的位置，确保了与核心预测数据的依赖关系正确，消除了潜在的变量作用域风险。
