# 版本 20260108.v1 更新说明

## 核心变更：产品流动性配置增强

本次更新主要集中在产品（基金）详情页的流动性规则配置功能，支持直接在前端修改并持久化保存基金的申赎规则。

### 1. 功能新增
- **产品详情页配置入口**：在基金详情页新增“配置流动性”按钮。
- **全局流动性弹窗**：将流动性配置模态框 (`LiquidityRuleModal`) 提升为全局组件，支持从任意页面调用。
- **无感刷新体验**：修改配置保存后，页面自动刷新数据而无需重新加载整个页面，提升用户体验。

### 2. 后端优化
- **数据库结构自动迁移**：
    - `funds` 表新增 `liquidity_rule_type`, `settlement_days`, `open_day` 等字段。
    - 服务启动时自动检查并修复数据库结构，无需手动运行 SQL 脚本。
- **接口增强**：
    - 新增 `PUT /api/funds/{id}` 接口，支持更新基金的流动性属性。
    - 修复了前后端字段命名风格不一致（SnakeCase vs CamelCase）导致的数据丢失问题。

### 3. 代码重构
- **App.tsx**：重构了路由和状态管理，修复了 Context 作用域问题。
- **Types**：更新了 TypeScript 类型定义，确保前后端类型安全。
- **UI交互优化**：
    - 在持仓列表的“个性化规则”标签上新增快捷清除按钮（[X]号），支持一键恢复产品默认规则。
    - 优化了 `App.tsx` 中的状态管理，实现规则清除后的即时反馈。
