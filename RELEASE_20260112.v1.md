# 版本 20260112.v1 更新说明

## 核心变更：客户数据同步与持仓穿透 (Data Sync & Navigation)

本次更新完成了客户列表与后端数据库的全面同步，并打通了“客户 -> 持仓”的导航路径，实现了真实数据的闭环管理。

### 1. 业务功能增强 (Business Features)
- **客户数据真实化**：
    - `Client List` 页面不再使用 Mock 数据，而是直接调取后端 `/api/clients` 接口。
    - 支持显示真实的 AUM（资产管理规模），该数值由客户名下的“现金余额 + 基金持仓 + 外部产品净值”实时聚合计算得出。
- **持仓穿透分析**：
    - **点击即跳转**：在客户列表中点击客户姓名，即可跳转至该客户专属的持仓分析页 (`/portfolio/:clientId`)。
    - **自动上下文切换**：系统会自动加载目标客户的持仓数据、交易记录及流动性规则，无需手动切换账户。
- **添加标签无遮挡**：修复了在客户列表中点击“添加标签”时，弹窗被表格容器遮挡的 UI 问题，体验更加流畅。

### 2. 后端架构升级 (Backend Improvements)
- **数据库 Schema 扩展**：
    - `clients` 表新增了完整画像字段：`phone`, `gender` (性别), `status` (状态), `risk_level` (风险等级), `last_contact_date` (最近联系), `tags` (JSON标签)。
    - 实现了**自动 Schema 迁移**，服务启动时会自动检测并添加缺失字段，无需手动执行 SQL。
- **接口扩展**：
    - 新增 `GET /api/clients`：支持返回富文本格式的客户列表。
    - 新增 `POST /api/clients`：支持录入新客户。
    - 升级 `GET /api/cash-flows`：增加 `client_id` 参数，支持按客户维度查询现金流。

### 3. API 文档更新 (Documentation)
- 更新了 `backend/API_DOCS.md`，补充了客户管理及现金流过滤的相关接口定义，确保文档与代码实现保持一致。
