# Release Note: v20260113.v3

## 1. 变更摘要 (Summary)
本次更新为“小版本发布”，主要关注前端开发环境的体验优化，规范了日志输出路径。

## 2. 详细变更 (Details)
- **[DevOps] 规范前端日志输出**
    - **修改**: 更新了 `package.json` 中的 `dev` 脚本。
    - **效果**: 执行 `npm run dev` 时，日志不再直接输出到控制台或根目录，而是自动重定向并保存至 `LOG/frontend.log`。
    - **目的**: 统一后端 (`LOG/backend.log`) 与前端的日志管理，保持项目根目录整洁。

## 3. 部署说明 (Deployment)
- **开发环境**: 
    - 拉取代码后，需重启 `npm run dev` 即可生效。
    - 旧的 `frontend.log` 文件已被自动归档备份。
- **生产环境**: 
    - 本次变更仅影响开发服务器启动命令，对生产环境构建 (`npm run build`) 无影响。

---
> **Version**: v20260113.v3
> **Date**: 2026-01-13
