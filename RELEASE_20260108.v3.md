# 版本 20260108.v3 更新说明

## 核心变更：流动性测算逻辑重构 (T+N 严谨模式)

本次更新对资产流动性的测算逻辑进行了深度重构，引入了严格的“前瞻性现金到账”模型，确保图表展示的流动性与实际资金到账时间完全一致。

### 1. 测算逻辑增强 (Logic Overhaul)
- **前瞻性原则**：测算严格基于“此时此刻”发起赎回。Day 0 (今日) 的 T+N 资产强制判定为锁定状态（因为昨日未发生赎回）。
- **现金到账机制**：
    - **每日开放基金**：按照 T+N 倒计时解锁。例如 T+3 基金，前3天锁定，第4天资金到账变为可用。
    - **每月开放基金**：采用“脉冲式”到账逻辑。仅在 (开放日 + 结算天数) 的那一天显示资金可用，精确反映资金到账窗口。
- **动态倒计时**：对于锁定期的每日开放基金，文案会动态显示剩余天数（如 `赎回结算中 (T+2到账)`）。

### 2. UI 交互优化 (Visual Improvements)
- **规则标签可视化**：资产列表新增了颜色区分的流动性规则标签（绿色-每日，琥珀色-每月，紫色-定期），一目了然。
- **文案精简**：
    - 去除了冗余的 "T+0" 描述。
    - 现金余额显示为 `实时可用`。
    - 已到账基金显示为 `每日开放` 或 `开放日赎回 (资金到账)`。

### 3. 文档建设 (Documentation)
- **新增模块规范**：在项目根目录新增 `LIQUIDITY_README.md`，详细定义了流动性测算的核心原则、资产规则及可视化规范，方便后续维护与查阅。

### 4. 修复与优化
- 修复了每月开放基金在非到账日错误显示为可用的 Bug。
- 优化了 Projection Loop 的计算性能，移除冗余的状态检查。
