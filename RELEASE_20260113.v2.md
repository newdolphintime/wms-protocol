# Release Note: v20260113.v2

## 1. 核心变更 (Core Changes)
本次更新主要增强了系统的可观测性和用户体验，新增了详细的 **API 日志记录**，并实现了 **客户搜索** 功能，大幅优化了多客户管理体验。

## 2. 业务功能增强 (Features)
- **[Feature] 客户搜索与持仓导航**
    - **搜索功能**: 在“持仓分析”页面，新增了客户搜索功能。支持通过**姓名**、**手机号**或**客户号 (ID)** 查找客户。
    - **动态路由**: 移除了默认加载 "client-001" 的硬编码逻辑。现在点击“持仓分析”默认进入搜索页，选择客户后进入该客户的持仓详情页 (`/portfolio/:clientId`)。
    - **体验优化**: 搜索支持防抖 (Debounce)，输入关键词后自动展示匹配结果。

## 3. 后端架构升级 (Backend)
- **[Log] 增强 API 日志中间件**
    - **全量日志**: 实现了对 HTTP 请求的全链路记录，包括 Request Body 和 Response Body (自动截断过长内容)。
    - **性能监控**: 日志中包含了每个请求的 **处理耗时 (Processing Time)**，便于性能分析。
    - **日志文件**: 日志同时输出到控制台和 `LOG/backend.log`。

- **[API] 客户查询接口增强**
    - `GET /api/clients` 新增 `keyword` 参数，支持后端模糊搜索。

## 4. 文档更新 (Documentation)
- **API Docs**: 更新了 `backend/API_DOCS.md`，补充了客户搜索接口参数说明。

---

> **部署说明**:
> 1. 拉取最新代码。
> 2. 重启后端服务: `python main.py` (以应用新的日志中间件)。
> 3. 前端无需特殊构建 (Dev 模式自动热更，生产环境需重新 Build)。
