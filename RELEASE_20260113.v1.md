# Release Note: v20260113.v1

## 1. 核心变更 (Core Changes)
本次发版重点解决了**多客户数据隔离**与**配置持久化**问题。
- 实现了严格的客户（Client）数据隔离，确保资金规划、交易记录互不干扰。
- 修复了流动性规则和资金规划数据在刷新后丢失的问题。

## 2. 业务功能增强 (Features & Fixes)
- **[Fix] 资金规划隔离**: 修复了“老张”的数据会出现在“老李”名下的严重漏洞。现在每笔资金规划都严格绑定客户ID。
- **[Feature] 规则持久化**: 持仓页面的“流动性配置”（如设置 T+7, 锁定期）现在会保存到数据库，不再因刷新页面而重置。
- **[Fix] 数据清理**: 清理了历史残留的无主数据（Ghost Data），保证系统数据纯净。

## 3. 后端架构升级 (Backend)
- **Schema 变更**: `cash_flows` 和 `recurring_rules` 表新增 `client_id` 字段。
- **API 新增**: 
    - `PUT /api/holdings/{id}`: 支持更新持仓配置。
- **API 优化**: 
    - `GET /api/cash-flows`: 强制要求 `client_id` 参数。
    - `POST /api/cash-flows/batch`: 支持顶层 `clientId` 注入。

## 4. 文档更新 (Documentation)
- **Database**: Updated `backend/DB_SCHEMA.md` to reflect new valid `client_id` columns.
- **API**: Updated `backend/API_DOCS.md` with new endpoint definitions.

## 5. 待办事项 (Next Steps)
- 建议管理员通知所有用户刷新页面以加载最新逻辑。
