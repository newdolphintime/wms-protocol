# Release Note: v20260113.v1

## 1. 核心变更 (Core Changes)
本次更新主要修复了系统稳定性问题，包括解决“外部资产录入白屏”和“持仓数据刷新失败”的严重 Bug，并完善了数据库结构与 API 文档。

## 2. 业务功能增强 (Features & Fixes)
- **[Fix] 解决“外部资产录入”白屏 (Critical)**
    - **现象**: 点击“录入外部资产”按钮后，页面崩溃变白。
    - **原因**: 后端兜底路由 (Catch-all) 遮挡了 `/api/external-products` 接口，导致前端收到 404 错误并解析失败。
    - **修复**: 调整了 `backend/main.py` 的路由注册顺序，确保 API 接口优先匹配。

- **[Fix] 优化持仓数据刷新体验**
    - **现象**: 新建或删除账户后，页面短暂显示“无法加载数据”错误。
    - **修复**: 改进了 `App.tsx` 中的 `PortfolioLoader` 刷新逻辑，使用后台静默更新代替清空数据，体验更流畅。

- **[Fix] 数据库字段补全**
    - **修复**: `schema.sql` 中 `accounts` 表缺失 `currency` 字段，导致部分环境重置后报错。

## 3. 后端架构升级 (Backend)
- **Schema Update**: `accounts` 表新增 `currency` 字段 (默认 `CNY`)。
- **Migration Script**: 新增 `backend/apply_migration.sql`，支持旧版本平滑升级。

## 4. 文档更新 (Documentation)
- **API Docs**: 更新 `backend/API_DOCS.md`，记录 v20260113.v1 版本变更。
- **DB Schema**: `backend/schema.sql` 已同步为最新结构。

---

> **部署说明**:
> 1. 拉取最新代码。
> 2. 如果是新环境，直接参考 `QUICK_DEPLOY.md` 初始化。
> 3. 如果是存量环境且需保留数据，请运行 `mysql -u root -p wms < backend/apply_migration.sql`。
> 4. 重启后端服务: `python main.py`。
